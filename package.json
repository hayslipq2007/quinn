{
  "name": "quinn",
  "version": "1.0.0",
  "description": "Vercel serverless API for Twilio phone number provisioning",
  "main": "api/buyNumber.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "twilio": "^4.10.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
